services:
  suveren:
    build: .
    ports:
      - "${SUVEREN_UI_PORT:-7400}:3000"
      - "${SUVEREN_MCP_PORT:-7430}:3030"
    volumes:
      - ${HOME}/.suveren:/app/data
    environment:
      - SUVEREN_AS_URL=https://www.suveren.ai
    network_mode: bridge
    restart: unless-stopped
