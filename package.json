{
  "name": "hap-platform",
  "version": "0.1.2",
  "private": true,
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  },
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm --filter @hap/ui dev & pnpm --filter @hap/control-plane dev & pnpm --filter @hap/mcp-server dev",
    "dev:ui": "pnpm --filter @hap/ui dev",
    "dev:mcp": "pnpm --filter @hap/mcp-server dev",
    "dev:control": "pnpm --filter @hap/control-plane dev",
    "test:e2e": "playwright test"
  },
  "dependencies": {
    "react-router-dom": "^7.13.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2"
  }
}
